<!DOCTYPE html>
<html>

<head>
    <title>Moja Express aplikacija</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    {{#if user}}
    <div class="p-2 bg-base-200 flex flex-row gap-4 flex-wrap">
        <div class="flex-1 flex flex-row gap-2 flex-wrap">
            <a class="btn btn-sm btn-accent" href="/competitions">Natjecanja</a>
        </div>
        <div class="flex-none flex flex-row justify-end items-baseline gap-2">
            <div>
                <a class="btn btn-sm btn-primary" href="/users/data">{{user.name}}</a>
            </div>
            <a class="btn btn-sm btn-accent" href="/users/signout">Odjava</a>
        </div>
    </div>
    {{/if}}
    {{{body}}}
</body>

</html>